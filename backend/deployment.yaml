apiVersion: apps/v1
kind: Deployment
metadata:
  name: fortune-cookie
  labels:
    app: fortune-cookie
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fortune-cookie
      tier: backend
      track: stable
  template:
    metadata:
      labels:
        app: fortune-cookie
        tier: backend
        track: stable
    spec:
      containers:
        - name: fortune-cookie
          image: